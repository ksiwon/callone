{#- callone: Qwen3.5 thinking 강제 OFF 채팅 템플릿 (ChatML + 빈 <think></think> 프리필).
    이유: 모델 임베디드 템플릿이 <think> 를 강제 주입해 enable_thinking:false/--reasoning-budget 0
    가 무시되는 llama.cpp 버그(#20182, 시스템 메시지 있으면 특히) → content 가 비고 응답 안 옴.
    해결: 어시스턴트 턴을 '<think>\n\n</think>\n\n' 로 프리필 = Qwen 공식 비-thinking 신호 →
    모델이 바로 답변 생성 → content 채워짐. 사용:  llama-server --jinja --chat-template-file 이파일 -#}
{%- for message in messages -%}
{{- '<|im_start|>' + message['role'] + '\n' + (message['content'] or '') + '<|im_end|>' + '\n' -}}
{%- endfor -%}
{%- if add_generation_prompt -%}
{{- '<|im_start|>assistant\n<think>\n\n</think>\n\n' -}}
{%- endif -%}
