# avatar-server(토킹헤드) 전용 의존성 — **별 venv `.venv-avatar`**.
#
# ⚠️ callone 서빙 venv(.venv-serve)와 **절대 섞지 마라**.
#    Ditto는 자체 TensorRT/torch 스택 → 충돌 → 별 프로세스(이 서버)로 격리.
#    (llama-server·callone-serve 와 동일 패턴: HTTP/WS 로만 호출, 파이썬 의존성 0.)
#
# ── 공통(서버 골격 + static 백엔드, CPU 에서도 동작) ──────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
numpy<2.4
pillow>=10.0
soundfile>=0.12

# ── Ditto 백엔드(GPU) — antgroup/ditto-talkinghead ───────────────────────
#   별도 절차(scripts/setup_avatar_gpu.sh): repo clone + HF checkpoints + TensorRT(Ampere+).
#   torch 는 CUDA 인덱스에서(드라이버 호환 버전 — callone 처럼 torch.version.cuda 맞춤).
# torch torchaudio   (--index-url https://download.pytorch.org/whl/cu124)
# librosa
# opencv-python-headless
# imageio
# imageio-ffmpeg
# scikit-image
# tensorrt==8.6.1            # Ampere(A100) TensorRT 백엔드. 비-Ampere 는 ONNX→TRT 변환(repo 스크립트)
# cuda-python
