# callone studio 통합 앱 — 최소 런타임(메뉴 + 전사 + 제로샷 TTS 배관)
# 무거운 백엔드(CosyVoice/llama-server/Qwen3-TTS/callone heavy)는 선택한 칸에서
# lazy import 되므로, 메뉴와 전사는 아래만으로 뜬다.
gradio>=4.0
soundfile>=0.12
pydub>=0.25
numpy>=1.24

# 전사(전사 패널 실제 동작)
faster-whisper>=1.0

# 제로샷 디노이즈 후보(선택)
noisereduce>=3.0

# 풀클론/통화는 callone 패키지 필요:
#   cd callone && pip install -e .            # 코어(배관/폴백)
#   cd callone && pip install -e ".[heavy]"   # 실모델 학습/추론(GPU 노드)
# 제로샷 TTS(CosyVoice3)는 voice_clone/<GPU_A100|CPU_laptop_WSL2>/setup.sh 로 설치.
#
# ── 알려진 함정(2026-06 웹 검증, 코드에 폴백 반영됨) ─────────────────────────
# 1) ffmpeg: 브라우저 마이크는 opus/webm → pydub 변환에 ffmpeg 필수.
#    Windows: choco install ffmpeg  (또는 ffmpeg/bin 을 PATH 등록).
#    ffmpeg 없으면 wav 업로드만 동작(soundfile 폴백).
# 2) faster-whisper GPU: ctranslate2>=4.5 는 CUDA 12.3 + cuDNN 9 필요.
#    torch 와 cuDNN 불일치 시 GPU 로드 실패 → 코드가 CPU int8 로 자동 폴백.
#    GPU 강제 쓰려면 torch(cu12x) 와 ctranslate2 버전 맞출 것.
# 3) Qwen3.5 = MoE: 학습은 bf16 LoRA 권장(QLoRA 4bit 비권장, bitsandbytes MoE 버그).
#    Windows 파인튜닝 불안정(unsloth #2742) → 학습은 Linux/A100.
